# מצפן ההדרכות — המבט המקצועי ומבט המורה

**תאריך:** 21.9.2026 · **מאושר על ידי:** מיטל פלג · **סטטוס:** אפיון לאישור

---

## למה

דף המדריכה (`hadrachot/guide/`) צמח שכבה על שכבה: שבע לשוניות, מסך סימון
נוכחות שמכיל שלוש דרכי סימון באותו מקום, ותוכנית שנתית שקשה לקרוא.
מיטל: *"עדיין המסך לא ברור ומעוצב באופן לא ידידותי למשתמש."*

**ארבע עובדות מהנתונים החיים שמכוונות את כל האפיון:**

| מה נבדק | התוצאה |
|---|---|
| כל רישומי הנוכחות במערכת | **44 מתוך 44 בסימון ידני של המדריכה** |
| מורים שנרשמו עצמאית | **0** |
| ייבוא דוח מהזום | **0 שימושים** |
| מורים עם נייד במערכת | **832 מתוך 864 (96%)** |
| מורים עם מייל | **476 מתוך 864 (55%)** |

**הערה על הנתון האחרון:** `teachers.list` הרחב מוחק את המייל וממסך את
הטלפון כשאין טוקן (`maskContact_`) — הגנת פרטיות שלנו. מדידה דרכו מחזירה
אפס ומטעה. המספרים כאן נמדדו לפי בית ספר, שאינו ממוסך.

## הפרדת שמות — תנאי מקדים

היום שני מסכים נושאים את השם "מבט מקצועי": `guide/` וגם `mabat/` (כך
בתיעוד הקוד). קביעת מיטל:

- **המבט המקצועי** = דף המדריכה. רק מדריכים.
- **מבט הפיקוח** = `mabat/` — ליאת צבר, יששכר חפץ, האחראים על מדריכים.
- **מבט המורה** = `teacher/`.

התיעוד וכותרות המסך יתוקנו. **הכתובות לא משתנות** — קישורים הופצו.

---

# שלב א — מבט המורה והרישום העצמי

עלה לראש הרשימה. הרישום העצמי הוא הדלת הראשית (קביעת מיטל), והוא לא
יכול לעבוד לפני שדף המורה תקין.

## א.1 שלוש תקלות שחוסמות הפצה

**1. הנוכחות שבורה.** נבדק על שיר כהן (עברית), מורה שבאמת נכח. הדף מציג:
*סה"כ הדרכות 0 · השתתפתי 0 · אחוז 0%*. הסיבה: `teacher/dashboard.js`
קורא `attendance.teacher` — **מערכת הנוכחות הישנה** — בעוד הנוכחות נרשמת
ב-`meetings`/`meeting_attendance`. זו אותה תקלה שתוקנה אצל המדריכה ב-18.9.
**מורה שייכנס ויראה 0% אחרי שהשתתף לא ייכנס שוב.**

**2. הכתובת היא מזהה המורה הגלוי** — `teacher/?id=tch_1786088165685_139`.
שינוי ספרה חושף נוכחות ופרטים של מורה אחר. יוחלף במפתח חתום, כמו
ב-`ishur-tochnit.html#k=` ובאישור המסלול של רכזי התקשוב.

**3. אף אחד לא מקשר לדף.** אפס קישורים באתר. נבנה ומעולם לא הופץ.

## א.2 טופס הכניסה

קביעת מיטל: *"בכניסה הראשונית כל מורה ממלא טופס שבו הוא כותב את המייל
שלו וכך הוא מגיע למבט המורה."*

**כתובת אחת לכל המורים** — המדריכה שולחת אותה לקהילת הווטסאפ שלה.
21 הודעות אישיות, לא שליחה המונית; לא נוגע בחסימה של 9.9.26.

```
┌──────────────────────────────────┐
│  מבט המורה · מצפן ההדרכות        │
│                                  │
│  השם שלך                         │
│  [ הקלד/י שתי אותיות...      ▾ ] │
│                                  │
│  המייל שלך                       │
│  [                             ] │
│                                  │
│  כדי שהנוכחות שלך תירשם          │
│  וההדרכה תיזקף לזכותך            │
│                                  │
│           [ כניסה ]              │
└──────────────────────────────────┘
```

- **בחירת השם מרשימה סגורה** של מורי המערכת (חיפוש לפי שתי אותיות),
  לא כתיבה חופשית — אותו כלל כמו בטופס המדריכה מ-17.9.
- **המייל נשמר לרשומת המורה** דרך `teachers.update`. כך נאספים 388
  המיילים החסרים בלי שמיטל או המנהלים ירדפו אחריהם.
- **מורה שהמייל שלו כבר במערכת** — הטופס ממלא אותו מראש לאישור.
- אחרי הכניסה **המכשיר זוכר**. מכאן ואילך פתיחה ישירה למבט המורה.
  החלפת מכשיר או ניקוי דפדפן = מילוי חוזר, פעם אחת.

**מה שהמייל כן ולא עושה — לרישום כן:** המייל **מזהה ולא מאמת**. מי
שיקליד מייל של עמיתה ייכנס לדף שלה. הוחלט לקבל את הסיכון — מדובר
בנוכחות בהדרכה, והחלופה (קישור אימות במייל) מוסיפה צעד שיפיל חלק
מהמורים בכניסה הראשונה. **הכתיבה מוגבלת לנוכחות ולפרטי קשר בלבד.**

## א.3 מבט המורה אחרי התיקון

```
שלום שיר כהן · עברית · גמר
─────────────────────────────────────
▸ ההדרכה היום — 15.9 בוקר
        [ אני כאן ✓ ]          ← רק ביום ההדרכה

▸ הנוכחות שלי
  ספטמבר   ✓ השתתפתי (15.9)
  אוקטובר  טרם התקיימה

▸ התעודה שלי
  38% · נדרש 80%   [ הפקת תעודה ]

▸ שאלה למדריכה
```

- **"אני כאן"** נשמר כ-`pending` וממתין לאישור המדריכה — כלל קיים של
  מיטל, ללא שינוי. `pending` אינו נספר כנוכחות עד שאושר.
- **התעודה היא הקרס.** מורה שרוצה תעודה צריך נוכחות; הדף מראה לו בדיוק
  איפה הוא עומד. זו הסיבה שיפתח את הקישור מיוזמתו.
- הנוכחות מחושבת ב-`assets/meet-stats.js` — אותו מנוע כמו כל המסכים.

---

# שלב ב — עמוד סימון הנוכחות של המדריכה

**כתובת:** `hadrachot/guide/nochechut.html?g=<slug>&date=<YYYY-MM-DD>&k=<key>`
דף נפרד ולא גלילה (בקשת מיטל). נפתח על **מועד בודד**.

```
‹ חזרה          15.9 בוקר · הדרכת ספטמבר            [?]
נושא: כתיבה יוצרת
────────────────────────────────────────────────────
[שליחת קישור לקהילה]                26 מתוך 86 סומנו

נרשמו בעצמם — ממתינים לאישורך (3)            [אישור הכל]
   טליה דדיה · עמל נצרת                      [✓] [✕]

רשימת הקבוצה                    [חיפוש...]
   אבישי אלקיים · תיכון הדר            [✓ נכח] [✕ לא]
   טליה דדיה · עמל נצרת                [✓ נכח] [✕ לא]
     ✓ השלימה את החודש (נכחה ב-16.9)
────────────────────────────────────────────────────
[שמירה]                           [סיום ההדרכה]
```

- **הסימון הידני נשאר במרחק לחיצה אחת.** היום הוא 100% מהרישומים; אם
  הרישום העצמי לא יתפוס, המדריכה לא תישאר בלי מסך שעובד.
- **"ממתינים לאישורך" מוצג רק כשיש בו משהו** — אחרת המסך פותח ברשימה.
- **הייבוא מהזום נמחק.** שמונה צעדים בשני אתרים, דורש הרשאת
  `Reports → Usage` בחשבון זום בתשלום שייתכן שלמדריכות אין, אפס שימושים.
- **[?] פותח חלון הסבר** — איך מסמנים, איך מורה נרשם לבד, ולמה נוכחות
  במועד אחד משלימה את החודש. ההסברים יוצאים מגוף המסך לחלון קופץ.

## ב.1 "סיום ההדרכה" — אימות נוכחות ולכידת שעות

קביעת מיטל: *"בסיום ההדרכה שהמדריכה תוודא שהיא מילאה נוכחות."*
המסך הזה עונה על זה **וגם** פותר את בעיית המונדיי (שלב ד).

```
סיום הדרכת 15.9
──────────────────────────────
26 מתוך 86 סומנו
60 שלא סומנו ייספרו כלא נכחו

[סמני את השאר כלא נכחו]  [חזרה לרשימה]

כמה זמן נמשכה ההדרכה?  [ 2 ] שעות

            [סיום ואישור]
```

**דורש שינוי שרת:** הוספת שדה `hours` לטאב `meetings`. היום אין בו שום
שדה משך — רק תאריך. בלי זה אי אפשר לייצר את דוח השעות.

---

# שלב ג — הדף הראשי של המדריכה

הסדר נקבע ע"י מיטל: חומרים → הדרכה קרובה → הדרכות קודמות.

```
מוריה פלינט · עברית · 3 יח"ל
86 מורים · 35 בתי ספר · החברה היהודית
─────────────────────────────────────────────
[חומרי הוראה] [שליחת חומרים] [הודעה לקבוצה]   ← כפתורים בהירים, קטנים
                        [+ הדרכה חדשה] [+ שעה פרטנית]

▸ ההדרכה הקרובה
  13.10 · ג׳ אחה"צ · מפגשי אוקטובר
  [פתיחת ההדרכה]

▸ ההדרכות הקודמות
  ┌──────────────────────────────────────────┐
  │ ספטמבר                            38%    │
  │ 33 נכחו · 53 חסרים                       │
  │ נושא: כתיבה יוצרת                        │
  │ ▾                                        │
  │    15.9 בוקר   26 נכחו   [סימון נוכחות]  │
  │    16.9 ערב    12 נכחו   [סימון נוכחות]  │
  │    חומרים קשורים: מצגת · דף עבודה        │
  │    החסרים (53): אבישי אלקיים · ...        │
  └──────────────────────────────────────────┘
  אוקטובר — טרם התקיימה
─────────────────────────────────────────────
לשוניות: [המורים שלי] [שעות פרטניות] [תוכנית שנתית] [סטטיסטיקה]
```

**סיכום חודשי שמכיל את שני המועדים** — בקשה מפורשת של מיטל.
**החסרים** = סך מורי הקבוצה פחות מי שהשתתף (*"לא צריך לסבך"*), לפי אנשים.

**מה יורד מהדף:**

| מה | לאן |
|---|---|
| לשונית "ההדרכות שלי" | נבלעת ב"הדרכות קודמות" — נוכחות · נושא · חומרים |
| לשונית "חומרים והודעות" | הכפתורים הבהירים בראש הדף |
| לשונית "נוכחות במפגשים" | העמוד הנפרד (שלב ב) |
| פאנל ייבוא מהזום | נמחק |

**"המורים שלי" נשארת כלשונית נפרדת** — לעריכה, הוספה וסימון רמת יח"ל.

**"+ שעה פרטנית"** ייפתח באותו חלון קופץ כמו "הדרכה חדשה". הטופס קיים
ואינו משתנה — הוא רק קבור היום בתוך לשונית.

**התוכנית השנתית** תהפוך לציר זמן ויזואלי: חודש אחרי חודש, המועדים של כל
אחד, וסימון ברור למה שהיה, מה היום ומה לפנינו.

---

# שלב ד — דוח שעות למונדיי

**הבעיה:** המדריכות ממלאות שעות במונדיי של מרמנט מדי חודש, ועושות את
העבודה פעמיים.

**מה לא נעשה:** כתיבה ישירה למונדיי של מרמנט. אין חשבון ואין טוקן.

**מה כן:** `guide_hours` כבר שומר תאריך · מורה · בית ספר · נושא · שעות
לכל שעה פרטנית, ואחרי שלב ב.1 יהיו שעות גם לכל מפגש קבוצתי. כפתור
**"דוח שעות ספטמבר"** יפיק טבלה אחת של כל מה שהמדריכה עשתה החודש, עם
העתקה וייצוא Excel. היא מדביקה במונדיי במקום לשחזר מהזיכרון.

**חסם לפני בנייה:** צריך את **העמודות האמיתיות של לוח המונדיי** — צילום
מסך או רשימת שדות מאחת המדריכות. בלי זה הפורמט ינוחש.

**נבדק ולא נבחר:** לכל לוח במונדיי כתובת מייל ייעודית לשליחת פריט. תלוי
בהגדרות והרשאות של מרמנט ושביר — לחזור אליו רק אם ההדבקה תתברר כמעיקה.

---

## כללי הספירה — ללא שינוי

הם החלטות של מיטל ואינם נוגעים לעיצוב:
חודש = הדרכה אחת · נוכחות באחד המועדים = נוכחות מלאה · `pending` אינו
נספר · שעה פרטנית מספקת את החודש שבו ניתנה (20.9.26) ·
מורה בגרות+גמר = אדם אחד.

## אימות

כל שלב נבדק ב-Playwright מול הנתונים החיים, **קריאה בלבד** — כל POST
מיורט, אפס כתיבות. 105 הבדיקות הקיימות חייבות להמשיך לעבור, ובראשן
`e2e-align.mjs` ששומרת שהמדריכה והמפקחת רואות אותם מספרים.

בדיקות חדשות: מבט המורה מציג נוכחות אמיתית למורה שנכח · טופס הכניסה
שומר מייל ומזהה נכון · "זוכר בפעם השנייה" · המספרים בהדרכות הקודמות
מול המנוע המשותף · מסך הסיום.

## מה לא בתחולה

- שינוי כללי הספירה.
- שינוי כתובות קיימות — קישורים הופצו.
- מסכי המנהל/ת, הרשת, הפיקוח והמבט הארצי — אחרי שהמבט המקצועי יתייצב.
- אימות מייל אמיתי (קישור לתיבה) — הוחלט לקבל את סיכון הזיהוי.
